
# 1.概念

1. 可以简单理解为<font color=red>数列的前i项的和</font>，是一种重要的预处理方式，一般是用来进行数组的预处理，提高后续操作效率的，多用于求区间和——<font color=red>所有数组下标从1开始</font>
	1. <font color=red>一维</font>：有n个正整数放在数组A里，现在要求一个新的数组sum，新数组的第i个数sum\[i]是原数组A第1到第i个数的和
		- ```
		  sum[1]=a[1];
		  sum[i]=sum[i-1]+a[i];
		  a[l]~~a[r]=sum[r]-sum[l-1];
		  ```
	2. <font color=red>二维</font>：有n* m个的正整数放到二维数组A中，现在要求一个新的二维数组sum，新数组sum\[i]\[j]是原数组A\[1]\[1]到A\[i]\[j]的和（矩形范围）
		- ![[Pasted image 20251114141328.png]]
		- ```
		  //sum数组初始化全为0
		  sum[1][1]=a[1][1];
		  sum[i][j]=sum[i-1][j]+sum[i][j-1]-sum[i-1][j-1]+a[i][j];
		  ```
		- ![[Pasted image 20251114141856.png]]
		- ```
		  a[x][y]~~a[i][j](x<=i,y<=j)
		  ans=sum[i][j]-sum[i][y-1]-sum[x-1][j]+sum[x-1][y-1]; 
		  ```
		- ![[Pasted image 20251114143126.png]]
2. 多维前缀和的普通求解方法几乎都是基于<font color=red>容斥原理</font>：
	- 在计数时，必须注意没有重复，没有遗漏。为了使重叠部分不被重复计算，人们研究出一种计数方法，，这种方法的基本思想是：先不考虑重叠的情况，把包含于某内容中的所有对象的数目先计算出来，然后再把计数时重复计算的数目排斥出去，使得计算的结果既无遗漏又无重复，这种计数的方法称为容斥原理
3. [求区间和](https:/luogu.com.cn/problem/B3612)
4. [最大正方形](https:/luogu.com.cn/problem/P1387)
	- 思路：
		1. 先枚举边长L，再枚举正方形左上角(i,j)，判断正方形内的元素和是否为正方形面积（元素不是0就是1），如果是则ans=max(L,ans)。时间复杂度O(n^5);
		2. <font color=red>考虑优化</font>：求矩形内元素和当然可以使用二维前缀和。也就是：sum为前缀和数组。a为矩阵元素。预处理：sum\[i]\[j]=sum\[i-1]\[j]+sum\[i]\[j-1]-sum\[i-1]\[j-1]+a\[i]\[j];然后就可以把o(n^5)优化到O(n^3)

# 2.技巧应用

## 2.1基础

[3152. 特殊数组 II - 力扣（LeetCode）](https://leetcode.cn/problems/special-array-ii/)

> 转换元素的含义再求前缀和。

```cpp
class Solution {

public:

    vector<bool> isArraySpecial(vector<int>& nums, vector<vector<int>>& queries) {

        int n=nums.size();

        vector<int>preSum(n,0);

        for(int i=1;i<n;i++)preSum[i]=preSum[i-1]+(nums[i-1]%2==nums[i]%2);

        vector<bool>ans;

        for(auto&query:queries)ans.push_back(preSum[query[1]]==preSum[query[0]]);

        return ans;

    }

};
```

[1749. 任意子数组和的绝对值的最大值 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-absolute-sum-of-any-subarray/)
```cpp
class Solution {

public:

    int maxAbsoluteSum(vector<int>& nums) {

        int n=nums.size();

        vector<int>preSum(n+1,0);

        int maxV=0,minV=0;

        for(int i=0;i<n;i++){

            preSum[i+1]=preSum[i]+nums[i];

            maxV=max(preSum[i+1],maxV);

            minV=min(preSum[i+1],minV);

        }

        return abs(maxV-minV);

    }

};
```


[3652. 按策略买卖股票的最佳时机 - 力扣（LeetCode）](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-using-strategy/)

> 用两个前缀和分别计算答案

```cpp
class Solution {

public:

    long long maxProfit(vector<int>& prices, vector<int>& strategy, int k) {

        int n=prices.size();

        vector<long long>sum(n+1,0),sumShell(n+1,0);

        for(int i=0;i<n;i++){

            sum[i+1]=sum[i]+strategy[i]*prices[i];

            sumShell[i+1]=sumShell[i]+prices[i];

        }

        long long ans=sum[n];

        for(int i=k;i<=n;i++){

            long long res=sum[i-k]+sum[n]-sum[i]+sumShell[i]-sumShell[i-k/2];

            ans=max(ans,res);

        }

        return ans;

    }

};
```

[3361. 两个字符串的切换距离 - 力扣（LeetCode）](https://leetcode.cn/problems/shift-distance-between-two-strings/submissions/707428145/)

> 处理环形数组的前缀和
```cpp
class Solution {

public:

    long long shiftDistance(string s, string t, vector<int>& nextCost, vector<int>& previousCost) {

        vector<long long>nextSum(26*2+1,0),preSum(26*2+1,0);

        for(int i=0;i<26*2;i++){

            nextSum[i+1]=nextSum[i]+nextCost[i%26];

            preSum[i+1]=preSum[i]+previousCost[i%26];

        }

        long long ans=0;

        for(int i=0;i<s.size();i++){

            int si=s[i]-'a',ti=t[i]-'a';

            long long res=0;

            ans+=min(nextSum[si>ti?ti+26:ti]-nextSum[si],

                    preSum[si<ti?si+26+1:si+1]-preSum[ti+1]);

        }

        return ans;

    }

};
```

## 2.2前缀和+哈希表

![[Pasted image 20260316202148.png]]

[974. 和可被 K 整除的子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/subarray-sums-divisible-by-k/)

> 前缀和+哈希表同时进行
> 处理负数取模

```cpp
class Solution {

public:

    int subarraysDivByK(vector<int>& nums, int k) {

        unordered_map<int,int>cnt;

        int ans=0,sum=0;

        for(auto&num:nums){

            cnt[sum]++;

            sum=((sum+num)%k+k)%k;

            ans+=cnt[sum];

        }

        return ans;

    }

};
```

[523. 连续的子数组和 - 力扣（LeetCode）](https://leetcode.cn/problems/continuous-subarray-sum/)

> 时刻记住`preSum[j]-preSum[i]`实际表示为`j+1->i`的和。

```cpp
class Solution {

public:

    bool checkSubarraySum(vector<int>& nums, int k) {

        unordered_map<int,int>cnt;

        int sum=0;

        cnt[0]=-1;

        for(int i=0;i<nums.size();i++){

            sum=(sum+nums[i])%k;

            if(cnt.contains(sum)){

                if(i-cnt[sum]>=2)return true;

            }

            else cnt[sum]=i;

        }

        return false;

    }

};
```

[525. 连续数组 - 力扣（LeetCode）](https://leetcode.cn/problems/contiguous-array/submissions/708125550/)

> 枚举右，维护左+前缀和
> 注意处理哈希表的第一项

```cpp
class Solution {

public:

    int findMaxLength(vector<int>& nums) {

        unordered_map<int,int>cnt;

        cnt[0]=-1;

        int s=0,ans=0;

        for(int i=0;i<nums.size();i++){

            s+=nums[i]?1:-1;

            if(cnt.contains(s))ans=max(ans,i-cnt[s]);

            else cnt[s]=i;

        }

        return ans;

    }

};
```

[3755. 最大平衡异或子数组的长度 - 力扣（LeetCode）](https://leetcode.cn/problems/find-maximum-balanced-xor-subarray-length/submissions/708441976/)

> 如何表示二元组pair<eor,sum>？
> 注意sum不能为负。
```cpp
class Solution {

public:

    int maxBalancedSubarray(vector<int>& nums) {

        int n=nums.size();

        unordered_map<long long,int>cnt;

        int eor=0,sum=n;

        int ans=0;

        for(int i=0;i<n;i++){

            eor^=nums[i];

            sum+=nums[i]%2?1:-1;

            long long res=((1LL*eor)<<32)|sum;

            if(cnt.contains(res))ans=max(ans,i-cnt[res]);

            else cnt[res]=i;

        }

        return ans;

    }

};
```

[3026. 最大好子数组和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-good-subarray-sum/)

> 前缀和做哈希表的value值

```cpp
class Solution {

public:

    long long maximumSubarraySum(vector<int>& nums, int k) {

        unordered_map<int,long long>cnt;

        long long s=0,ans=LLONG_MIN;

        for(auto&num:nums){

            s+=num;

            auto it=cnt.find(num-k);

            if(it!=cnt.end())ans=max(ans,s-cnt[num-k]+num-k);

            it=cnt.find(num+k);

            if(it!=cnt.end())ans=max(ans,s-cnt[num+k]+num+k);

            it=cnt.find(num);

            if(it==cnt.end()||cnt[num]>s)cnt[num]=s;

  

        }

        return ans==LLONG_MIN?0:ans;

    }

};
```

> 前缀和+排序
> 前缀和与后缀和技巧相同

```cpp
#include<vector>

#include<iostream>

#include<algorithm>

using namespace std;

  

int t;

int arr[200005];

int n;

  

void solve(){

    long long sum=0;

    for(int i=1;i<=n;i++)sum+=arr[i];

    long long cur=0;

    vector<long long>suf;

    for(int i=n;i>=2;i--)suf.push_back(cur+=arr[i]);

    sort(suf.begin(),suf.end(),greater());

    cout<<sum;

    for(int i=0;i<n-1;i++){

        sum+=suf[i];

        cout<<" "<<sum;

    }

    cout<<endl;

}

  

int main(){

    ios::sync_with_stdio(false);

    cin.tie(0);

    cin>>t;

    while(t--){

        cin>>n;

        for(int i=1;i<=n;i++)cin>>arr[i];

        solve();

    }

    return 0;

}
```



[P9749 [CSP-J 2023] 公路 - 洛谷](https://www.luogu.com.cn/problem/P9749#ide)
```cpp
#include<iostream>
using namespace std;

const int N=1e5+5;
using ll=long long;
int n,d,minV=N;
ll sum,ans;
int v[N],a[N];

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin>>n>>d;
    for(int i=1;i<n;i++)cin>>v[i];
    for(int i=1;i<n;i++){
        cin>>a[i];
        minV=min(minV,a[i]);
        sum+=v[i];
        if(sum>0){
            ans+=(sum+d-1)/d*minV;
            sum-=(sum+d-1)/d*d;
        }
    }
    cout<<ans<<endl;
    return 0;
}
```
