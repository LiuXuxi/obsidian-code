# 1.是什么

> [!ABSTRACT]
> CMake是用于构建、测试和软件打包的开源跨平台工具

# 2.为什么选用cmake
- 避免硬编码
- 需要在多台计算机上构建一个包
- 使用CI（持续集成）
- 支持不同的操作系统
- 支持多个编译器
- 描述程序的逻辑结构，而不是标志和命令
- 使用库（第三方库等）
- 使用其他工具来帮助编写代码 moc ProtoBuf
- 使用单元测试

# 3.持续集成

- 每次集成都通过自动化的制造（包括提交、发布、自动化测试）来验证，准确地发现集成错误。
- 